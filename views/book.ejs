<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book a Trial - Elder</title>
    <link rel="icon" type="image/x-icon" href="/img/elder.ico" />
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/book.css" />
</head>

<body>
    <%- include('partials/loader') %>
        <div class="main">
            <%- include('partials/header') %>

                <section class="book-section">
                    <div class="book-container">
                        <h2>Book Now</h2>
                        <form action="/book" method="POST">
                            <input type="text" name="name" value="<%= user?.fullname %>" readonly />
                            <input type="email" name="email" value="<%= user?.email %>" readonly />

                            <select name="service" required>
                                <option value="">-- Select Service --</option>
                                <option value="Companionship">Companionship</option>
                                <option value="Home Nursing">Home Nursing</option>
                                <option value="Grocery Support">Grocery Support</option>
                                <option value="Mobility Assistance">Mobility Assistance</option>
                                <option value="Health Monitoring">Health Monitoring</option>
                                <option value="Medical Reminders">Medical Reminders</option>
                            </select>

                            <input type="date" name="date" id="dateInput" required />
                            <input type="time" name="time" id="timeInput" required />

                            <textarea name="note" placeholder="Any special requests or health concerns"></textarea>
                            <button type="submit">Confirm Booking</button>
                        </form>

                        <div class="booking-flash" id="flashMessage" data-success="<%= success ? success : '' %>"
                            data-error="<%= error ? error : '' %>"></div>

                        <br>
                        <h3>Your Bookings</h3>
                        <table class="booking-table">
                            <thead>
                                <tr>
                                    <th>Date</th>
                                    <th>Time</th>
                                    <th>Service</th>
                                    <th>Requested Date</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (bookingList && bookingList.length> 0) { %>
                                    <% bookingList.forEach(function(booking){ %>
                                        <tr>
                                            <td>
                                                <%= booking.date %>
                                            </td>
                                            <td>
                                                <%= booking.time %>
                                            </td>
                                            <td>
                                                <%= booking.service %>
                                            </td>
                                            <td>
                                                <%= booking.requestedDate || '-' %>
                                            </td>
                                            <td><span class="status <%= booking.status %>">
                                                    <%= booking.status %>
                                                </span></td>
                                            <td>
                                                <% if (booking.status !== 'cancelled' && booking.status !== 'rejected') { %>
                                                    <form action="/bookings/<%= booking.id %>/cancel" method="POST"
                                                        class="booking-action-form">
                                                        <button type="submit" class="cancel-btn">Cancel</button>
                                                    </form>
                                                    <% } else { %>
                                                        <span class="booking-action-muted">-</span>
                                                        <% } %>
                                            </td>
                                        </tr>
                                        <% }); %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="6" style="text-align:center;padding:15px;color:#666">
                                                        You have no booking yet.
                                                    </td>
                                                </tr>
                                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </section>

                <%- include('partials/footer') %>
        </div>

        <script src=/js/toggle.js></script>
        <script src=/js/book.js></script>

</body>

</html>